package application;

import application.database.DBProvider;
import application.database.DatabaseQueryException;
import application.model.Item;

public class Controller {


    /**
     * Fill the Item
     *
     * @param Item to be filled
     * @param dbProvider a connected dbProvider to find the relevant information
     */
//    protected void fillItem(Item item, DBProvider dbProvider) throws DatabaseQueryException {
//        item.setItem(dbProvider.getItem(item.getItemID()));
//        item.setQuantity(dbProvider.getItem(item.getItemID()).getQuantity());
//        item.setName(dbProvider.getItem(item.getItemID()).getName());
//        item.setDescription(dbProvider.getItem(item.getItemID()).getDescription());
//
//        System.out.println("Done fillItem(), sample setName: " + dbProvider.getItem(item.getItemID()).getName());
//    }



}
